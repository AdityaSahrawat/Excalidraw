1. ws sending same shape to creator of the shape  = done

2. If someone is building a shape and someone sends shape using ws then this will be removed 
(due to clearCanvas fn) = pending

3. on react strict mode , due to double reload two canvas ref are created which cause
 twice mouseup and mousedown in sigle event = disabled reactstrictmode for now = pending

4. add types to messages in ws = pending

5. add resizing = done

6. line/arrow selection and resizing error fix

7. auto selection of created shape = done

8. add text

9. add current zoom value = done

10. add transparent shape feature =

11. solve 10000... bugs!!

